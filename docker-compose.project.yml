services:
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: psa
      MINIO_ROOT_PASSWORD: psa12345
    ports:
      - "19000:9000"
      - "19001:9001"
    volumes:
      - project_minio_data:/data
    networks:
      - psa_network

  mongo:
    image: mongo:7.0
    init: true
    ports:
      - "16521:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: psa
      MONGO_INITDB_ROOT_PASSWORD: psa
    volumes:
      - project_mongo_data:/data/db
    networks:
      - psa_network

  floodcast:
    build:
      context: ./backend
      dockerfile: Dockerfile
    entrypoint: ["python", "-m", "floodcast.main"]
    working_dir: /workspace/backend
    environment:
      MONGO_URI: mongodb://psa:psa@mongo:27017/?authSource=admin
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: psa
      MINIO_SECRET_KEY: psa12345
      MINIO_BUCKET: psa
      MINIO_SECURE: "false"
      PYTHONPATH: /workspace/backend/floodcast
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      OPENROUTER_EXPLANATION_MODEL: ${OPENROUTER_EXPLANATION_MODEL:-google/gemini-2.5-flash-lite}
      OPENROUTER_EXPLANATION_TIMEOUT_SECONDS: ${OPENROUTER_EXPLANATION_TIMEOUT_SECONDS:-8.0}
    volumes:
      - ./:/workspace
    depends_on:
      - minio
      - mongo
    networks:
      - psa_network

  harvest-scheduler:
    build:
      context: ./backend
      dockerfile: Dockerfile
    entrypoint: ["psa-harvest-scheduler", "--top-of-hour", "--run-immediately"]
    working_dir: /workspace/backend
    environment:
      MONGO_URI: mongodb://psa:psa@mongo:27017/?authSource=admin
      CONFIG_DB_NAME: harvest_config
      DATA_DB_NAME: api_data
      DEFESA_CIVIL_API_ID: ${DEFESA_CIVIL_API_ID}
      DEFESA_CIVIL_SISTEMA_ID: ${DEFESA_CIVIL_SISTEMA_ID:-2}
      PYTHONPATH: /workspace/backend/harvest
    volumes:
      - ./:/workspace
    depends_on:
      - mongo
    networks:
      - psa_network

  harvest:
    build:
      context: ./backend
      dockerfile: Dockerfile
    entrypoint: ["psa-harvest"]
    working_dir: /workspace/backend
    environment:
      MONGO_URI: mongodb://psa:psa@mongo:27017/?authSource=admin
      CONFIG_DB_NAME: harvest_config
      DATA_DB_NAME: harvest_data
      OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY}
      DEFESA_CIVIL_API_ID: ${DEFESA_CIVIL_API_ID}
      DEFESA_CIVIL_SISTEMA_ID: ${DEFESA_CIVIL_SISTEMA_ID:-2}
      PYTHONPATH: /workspace/backend/harvest
    volumes:
      - ./:/workspace
    depends_on:
      - mongo
    networks:
      - psa_network

  harvest-seed:
    build:
      context: ./backend
      dockerfile: Dockerfile
    entrypoint: ["psa-seed-harvest-config"]
    working_dir: /workspace/backend
    environment:
      MONGO_URI: mongodb://psa:psa@mongo:27017/?authSource=admin
      CONFIG_DB_NAME: harvest_config
      PYTHONPATH: /workspace/backend/harvest
    volumes:
      - ./:/workspace
    depends_on:
      - mongo
    networks:
      - psa_network

  sentry:
    build:
      context: ./backend
      dockerfile: Dockerfile
    entrypoint: []
    working_dir: /workspace/backend/sentry
    command: ["python", "source/run.py", "--env", "local"]
    environment:
      MONGO_URI: mongodb://psa:psa@mongo:27017/?authSource=admin
      MONGO_GITLY_CONNECTION_STRING: mongodb://psa:psa@mongo:27017/?authSource=admin
      MONGO_GITLY_VERSION_SCHEMA: "1"
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: psa
      MINIO_SECRET_KEY: psa12345
      MINIO_BUCKET: psa
      MINIO_SECURE: "false"
      PYTHONPATH: /workspace/backend/floodcast:/workspace/backend/harvest:/workspace/backend/sentry/source
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      OPENROUTER_EXPLANATION_MODEL: ${OPENROUTER_EXPLANATION_MODEL:-google/gemini-2.5-flash-lite}
    volumes:
      - ./:/workspace
    depends_on:
      - mongo
    ports:
      - "8080:8080"
    networks:
      - psa_network

  frontend:
    build:
      context: ./frontend-v3
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api
        VITE_USE_MOCK_DATA: "false"
    ports:
      - "8081:80"
    depends_on:
      - sentry
    networks:
      - psa_network

networks:
  psa_network:

volumes:
  project_minio_data:
  project_mongo_data:
